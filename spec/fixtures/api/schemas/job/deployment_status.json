{
  "type": "object",
  "required": [
    "status",
    "environment"
  ],
  "properties": {
    "status": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "last",
            "creating",
            "failed",
            "out_of_date",
            "canceled"
          ]
        },
        {
          "type": "null"
        }
      ]
    },
    "environment": {
      "$ref": "../environment.json"
    },
    "action": {
      "type": "string",
      "enum": [
        "start",
        "stop",
        "prepare",
        "verify",
        "access"
      ]
    }
  },
  "additionalProperties": false
}
