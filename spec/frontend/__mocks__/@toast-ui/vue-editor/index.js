export const Editor = {
  props: {
    initialValue: {
      type: String,
      required: true,
    },
  },
  render(h) {
    return h('div');
  },
};

export const Viewer = {
  render(h) {
    return h('div');
  },
};
