# `spec:description` is limited to 256 characters
spec:
  description: "This description is deliberately longer than the 256 character limit enforced by the spec header entry, so the schema must reject it. This description is deliberately longer than the 256 character limit enforced by the spec header entry, so the schema must reject it. This description is deliberately"
