= render RapidDiffs::DiffFileComponent.new(diff_file:, parallel_view: @parallel_view, plain_view: @plain_view) do |c|
  - c.with_header do
    = render RapidDiffs::DiffFileHeaderComponent.new(diff_file:, additional_menu_items:)
  end
