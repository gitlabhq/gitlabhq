{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Holds Topology Service settings",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "topology_service_concurrency_limit": {
      "type": "integer",
      "minimum": 1,
      "default": 40,
      "description": "Maximum number of concurrent in-flight requests allowed to the Topology Service. The default of 40 yields a peak of ~200 req/sec given the 200ms per-request timeout (each connection can issue up to 5 requests per second)."
    }
  }
}
