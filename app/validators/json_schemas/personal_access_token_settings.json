{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Personal access token settings",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "enforce_granular_tokens": {
      "type": "boolean",
      "default": false,
      "description": "Whether fine-grained personal access tokens are enforced."
    },
    "granular_tokens_enforced_after": {
      "type": [
        "string",
        "null"
      ],
      "format": "date",
      "description": "On self-managed, date after which only fine-grained personal access tokens can be created, existing legacy tokens remain valid. On SaaS, legacy tokens are blocked from accessing group resources after this date."
    }
  }
}
