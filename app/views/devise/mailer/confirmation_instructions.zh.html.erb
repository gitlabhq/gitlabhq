<p>Welcome <%= @resource.name %>!</p>

<% if @resource.unconfirmed_email.present? %>
  <p>您可以通过以下链接确认您的email (<%= @resource.unconfirmed_email %>):</p>
<% else %>
  <p>您可以通过以下链接确认您的帐号:</p>
<% end %>

<p><%= link_to '确认我的帐号', confirmation_url(@resource, confirmation_token: @token) %></p>
