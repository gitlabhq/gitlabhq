= render Pajamas::CardComponent.new(card_options: { class: 'gl-mb-5' }, body_options: { class: 'gl-py-2'}) do |c|
  - c.with_header do
    = _('Account')
  - c.with_body do
    %ul.content-list
      = render 'admin/users/basic_details'
