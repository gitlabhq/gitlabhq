
.row-content-block.second-block.center
  / %h3.page-title
  /   Koding Dashboard
  - if Gitlab.config.koding.enabled
    %p
      = icon('circle', class: 'cgreen')
      Integration is active for
      = link_to koding_project_url, target: '_blank' do
        #{Gitlab.config.koding.url}
  - else
    %p
      = icon('circle', class: 'cred')
      Integration is not active, you can activate it by providing your Koding URL in the settings.

// Temporarily show the video of integration, we eventually will update
// this page for the integration details ~GG
- video_base_url = "http://www.koding.com/hubfs/koding-gitlab/media/GitLab-Koding"
%div{ class: container_class }
  .koding-video
    %video{ preload: "auto", autoplay: 'true', width: 689, height: 465 }
      %source{ type: 'video/mp4;codecs="avc1.42E01E, mp4a.40.2"', src: "#{video_base_url}.mp4" }
      %source{ type: 'video/webm;codecs="vp8, vorbis"', src: "#{video_base_url}.webm" }
      %source{ type: 'video/ogg;codecs="theora, vorbis"', src: "#{video_base_url}.ogg" }
      I'm sorry; your browser doesn't support HTML5 video in WebM with VP8 or MP4 with H.264.
  .koding-video-title
    Koding integration sneak preview
