- if @has_authorized_projects
  .projects
    .activities.span8
      = render 'activities'
    .side
      = render 'sidebar'

- else
  = render "zero_authorized_projects"
:javascript
  $(function(){ Pager.init(20); });
