- page_title _("Custom Emoji")


.top-area.adjust
  .nav-text
    = _("Custom Emoji can be used in for example titles, descriptions and notes for both issues and merge requests. They can also be used as award emoji.")

  .nav-controls
    = link_to new_custom_emoji_url, class: "btn btn-new" do
      = _('New custom emoji')

.table-responsive
  %table.table
    %thead
      %tr
        %th
          = s_("CustomEmoji|Image")
        %th
          = s_("CustomEmoji|Name")
        %th
    - @custom_emoji.each do |emoji|
      %tr
        %td= custom_emoji_icon(emoji.name, emoji.url)
        %td= emoji.name
        %td
          = link_to profile_custom_emoji_path(emoji), method: :delete, class: 'btn btn-remove pull-right', aria: { label: _('Delete') } do
            = icon('trash', class: 'hidden-xs')
