%li
  .file_holder
    .file_title
      = link_to project_wiki_path(@project,blob.filename, :anchor => "L" + blob.startline.to_s) do
        %i.icon-file
        %strong
          = blob.filename
    .file_content.code.term
      %div{class: user_color_scheme_class}
        = raw blob.colorize( formatter: :gitlab, options: { first_line_number: blob.startline } )
