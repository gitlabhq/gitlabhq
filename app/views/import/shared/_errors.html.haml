- if @errors.present?
  = render Pajamas::AlertComponent.new(variant: :danger,
    dismissible: false,
    alert_class: 'gl-mb-5') do
    .gl-alert-body
      - @errors.each do |error|
        = error
