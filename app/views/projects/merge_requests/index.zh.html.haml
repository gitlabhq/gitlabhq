.append-bottom-10
  .pull-right
    = render 'shared/issuable_search_form', path: namespace_project_merge_requests_path(@project.namespace, @project)

    - if can? current_user, :write_merge_request, @project
      = link_to new_namespace_project_merge_request_path(@project.namespace, @project), class: "btn btn-new pull-left", title: "新建合并请求" do
        %i.fa.fa-plus
        新建合并请求
  = render 'shared/issuable_filter'
.merge-requests-holder
  = render 'merge_requests'
