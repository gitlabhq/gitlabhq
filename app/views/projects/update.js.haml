- if @project.valid?
  :plain
    location.href = "#{edit_project_path(@project, :notice => t('projects.index.updated'))}";
- else
  :plain
    $('.project_edit_holder').show();
    $(".edit_project").replaceWith("#{escape_javascript(render('form'))}");
    $('.ajax_loader').hide();
