- if current_user
  = link_to toggle_star_namespace_project_path(@project.namespace, @project), class: 'btn star-btn toggle-star has-tooltip', method: :post, remote: true, title: "星标项目" do
    - if current_user.starred?(@project)
      = icon('star fw')
      %span.starred 取消星标
    - else
      = icon('star-o fw')
      %span 星标
  %div.count-with-arrow
    %span.arrow
    %span.count.star-count
      = @project.star_count

- else
  = link_to new_user_session_path, class: 'btn has-tooltip star-btn', title: '必须登录后才能给项目加注星标' do
    = icon('star fw')
    星标
  %div.count-with-arrow
    %span.arrow
    %span.count
      = @project.star_count
