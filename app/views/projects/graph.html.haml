%h2.icon
  %span>
  Network Graph
.clear
#holder.graph

:javascript
  var chunk1={commits:#{@commits_json}};
  var days=#{@days_json};
  initGraph();
  $(function(){
    branchGraph($("#holder")[0]);
  });
