- page_title "Webhooks"
= render "projects/settings/nav"

= render 'shared/web_hooks/form', hook: @hook, hooks: @hooks, url_components: [@project.namespace.becomes(Namespace), @project]

%hr

= render "projects/services/index"
