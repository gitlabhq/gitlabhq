- title = s_('AddPasskey|Add passkey')
- breadcrumb_title title
- page_title title, s_('ProfilesAuthentication|Password and authentication')
- add_to_breadcrumbs s_('ProfilesAuthentication|Password and authentication'), profile_two_factor_auth_path

%h1
  = s_('AddPasskey|Add passkey')

%p
  - help_link = link_to '', help_page_path('auth/passkeys.md'), target: '_blank', rel: 'noopener noreferrer'
  = safe_format(s_('AddPasskey|Follow the instructions on your browser to add a passkey. %{link_start}Learn more about passkeys%{link_end}.'), tag_pair(help_link, :link_start, :link_end))

#js-passkey-registration{ data: device_registration_data(current_password_required: current_password_required?, target_path: profile_passkeys_path, webauthn_error: @webauthn_error) }
