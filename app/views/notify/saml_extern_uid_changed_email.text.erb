Hi <%= sanitize_name(@user.name) %>!

The SAML authentication identifier (extern_uid) linked to your account was changed by an owner of the <%= @group_name %> group.

As a security measure, you must sign in with your GitLab credentials and re-link your SAML identity before you can use SAML single sign-on again.

If you did not expect this change, contact your group owner or a GitLab administrator immediately.
