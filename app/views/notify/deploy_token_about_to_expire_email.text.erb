<%= _('Hi %{username}!') % { username: sanitize_name(@user.name) } %>

<%= _('Your project deploy token %{token_name} for %{project_path} will expire in %{days_to_expire} or less.') % { days_to_expire: pluralize(@days_to_expire, _('day')), token_name: @token_name, project_path: @project.full_path } %>

<%= _('You can create a new one or check them in your deploy tokens settings: %{target_url}') % { target_url: @target_url } %>

<%= _('You are receiving this email because you are %{role} of the project.') % { role: @user_role } %>
