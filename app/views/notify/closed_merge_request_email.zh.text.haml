= "#{@updated_by.name}关闭了合并请求##{@merge_request.iid}"

合并请求url: #{namespace_project_merge_request_url(@merge_request.target_project.namespace, @merge_request.target_project, @merge_request)}

= merge_path_description(@merge_request, 'to')

作者: #{@merge_request.author_name}
指定给: #{@merge_request.assignee_name}
