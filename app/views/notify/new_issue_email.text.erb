<%= _('%{user} created an issue: %{issue_link}') % { user: sanitize_name(@issue.author_name),
                                                     issue_link: @target_url } %>

<%= assignees_label(@issue) if @issue.assignees.any? %>

<%= @issue.description %>
