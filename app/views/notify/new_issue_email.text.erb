<%= _('%{user} created an %{type}: %{issue_link}') % { user: sanitize_name(@issue.author_name),
                                                       issue_link: @target_url,
                                                       type: work_item_type_for(@issue) } %>

<%= assignees_label(@issue) if @issue.assignees.any? %>

<%= @issue.description %>
