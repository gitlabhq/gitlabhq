# @feature_category: mcp_server
mutation updateWorkItemMcp($input: WorkItemUpdateInput!) {
  workItemUpdate(input: $input) {
    workItem {
      id
      iid
      title
      state
      confidential
      webUrl
      workItemType {
        id
        name
      }
    }
    errors
  }
}
