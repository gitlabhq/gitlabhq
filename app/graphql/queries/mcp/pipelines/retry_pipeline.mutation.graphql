# @feature_category: mcp_server
mutation mcpRetryPipeline($input: PipelineRetryInput!) {
  pipelineRetry(input: $input) {
    pipeline {
      id
      status
      ref
      project {
        id
        webUrl
      }
    }
    errors
  }
}
