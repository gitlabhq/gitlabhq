# @feature_category: mcp_server
mutation mcpCancelPipeline($input: PipelineCancelInput!) {
  pipelineCancel(input: $input) {
    pipeline {
      id
      status
      ref
      project {
        id
        webUrl
      }
    }
    errors
  }
}
