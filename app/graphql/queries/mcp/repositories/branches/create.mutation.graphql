# @feature_category: mcp_server
mutation addBranch($input: CreateBranchInput!) {
  createBranch(input: $input) {
    branch {
      name
      commit {
        id
        sha
      }
    }
    errors
  }
}
