#import "./work_item_ancestors.fragment.graphql"

# @feature_category: portfolio_management
query workItemAncestorsQuery($id: WorkItemID!, $useWorkItemFeatures: Boolean = false) {
  workItem(id: $id) {
    ...WorkItemAncestors
  }
}
