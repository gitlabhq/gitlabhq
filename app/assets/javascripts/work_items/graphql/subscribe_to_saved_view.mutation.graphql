# @feature_category: portfolio_management

mutation subscribeToSavedView($input: WorkItemSavedViewSubscribeInput!) {
  workItemSavedViewSubscribe(input: $input) {
    savedView {
      id
    }
    errors
  }
}
