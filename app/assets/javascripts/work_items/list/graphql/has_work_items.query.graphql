# @feature_category: portfolio_management
query hasWorkItems($fullPath: ID!, $workItemTypeIds: [WorkItemsTypeID!]) {
  namespace(fullPath: $fullPath) {
    id
    workItems(workItemTypeIds: $workItemTypeIds, first: 1) {
      nodes {
        id
      }
    }
  }
}
