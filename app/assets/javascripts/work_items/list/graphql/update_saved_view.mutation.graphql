# @feature_category: portfolio_management

#import "./saved_view.fragment.graphql"

mutation workItemSavedViewUpdate($input: WorkItemSavedViewUpdateInput!) {
  workItemSavedViewUpdate(input: $input) {
    savedView {
      ...SavedView
    }
    errors
  }
}
