# @feature_category: portfolio_management

mutation unsubscribeFromSavedView($id: WorkItemsSavedViewID!) {
  unsubscribeFromSavedView(id: $id) @client {
    errors
    savedView {
      id
    }
  }
}
