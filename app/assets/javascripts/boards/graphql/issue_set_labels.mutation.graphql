mutation issueSetLabels($input: UpdateIssueInput!) {
  updateIssue(input: $input) {
    issue {
      id
      labels {
        nodes {
          id
          title
          color
          description
        }
      }
    }
    errors
  }
}
