export default ({
  members,
  sourceId,
  currentUserId,
  tableFields,
  tableAttrs,
  memberPath,
  requestFormatter,
}) => ({
  members,
  sourceId,
  currentUserId,
  tableFields,
  tableAttrs,
  memberPath,
  requestFormatter,
  showError: false,
  errorMessage: '',
  removeGroupLinkModalVisible: false,
  groupLinkToRemove: null,
});
