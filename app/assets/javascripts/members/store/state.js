export default ({
  members,
  sourceId,
  currentUserId,
  canManageMembers,
  tableFields,
  tableAttrs,
  tableSortableFields,
  memberPath,
  requestFormatter,
  filteredSearchBar,
}) => ({
  members,
  sourceId,
  currentUserId,
  canManageMembers,
  tableFields,
  tableAttrs,
  tableSortableFields,
  memberPath,
  requestFormatter,
  filteredSearchBar,
  showError: false,
  errorMessage: '',
  removeGroupLinkModalVisible: false,
  groupLinkToRemove: null,
});
