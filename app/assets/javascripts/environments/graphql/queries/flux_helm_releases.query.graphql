query getFluxHelmReleasesQuery(
  $configuration: LocalConfiguration
  $namespace: String
  $version: String
) {
  fluxHelmReleases(configuration: $configuration, namespace: $namespace, version: $version)
    @client {
    apiVersion
    metadata {
      name
      namespace
    }
  }
}
