query getFluxKustomizationsQuery(
  $configuration: LocalConfiguration
  $namespace: String
  $version: String
) {
  fluxKustomizations(configuration: $configuration, namespace: $namespace, version: $version)
    @client {
    apiVersion
    metadata {
      name
      namespace
    }
  }
}
