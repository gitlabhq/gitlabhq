mutation takeOwnership($id: CiPipelineScheduleID!) {
  pipelineScheduleTakeOwnership(input: { id: $id }) {
    pipelineSchedule {
      id
      owner {
        id
        name
      }
    }
    errors
  }
}
