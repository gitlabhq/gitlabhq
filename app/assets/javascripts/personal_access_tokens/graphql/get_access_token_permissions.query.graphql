query getAccessTokenPermissions {
  accessTokenPermissions {
    name
    action
    description
    category
    categoryName
    resource
    resourceName
    resourceDescription
    boundaries
  }
}
