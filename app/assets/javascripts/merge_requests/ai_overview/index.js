// FOSS fallback for `ee_else_ce/merge_requests/ai_overview`. The overview replacement is EE only,
// and its mount point is never rendered here, so this is only ever resolved, never called.
export default () => null;
