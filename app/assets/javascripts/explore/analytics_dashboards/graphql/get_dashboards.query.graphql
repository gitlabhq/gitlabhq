#import "~/graphql_shared/fragments/user.fragment.graphql"

# @feature_category: custom_dashboards_foundation
query getDashboards($organizationId: OrganizationsOrganizationID!) {
  customDashboards(organizationId: $organizationId) {
    nodes {
      id
      name
      description
      config
      organization {
        id
      }
      namespace {
        id
        name
      }
      createdBy {
        ...User
      }
      createdAt
      updatedAt
    }
  }
}
