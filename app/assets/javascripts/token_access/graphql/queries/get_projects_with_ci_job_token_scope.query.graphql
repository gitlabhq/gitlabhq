query getProjectsWithCIJobTokenScope($fullPath: ID!) {
  project(fullPath: $fullPath) {
    ciJobTokenScope {
      projects {
        nodes {
          name
          fullPath
        }
      }
    }
  }
}
