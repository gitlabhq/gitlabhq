#import "../fragments/container_tags_expiration_policy.fragment.graphql"

mutation updateContainerTagsExpirationPolicy($input: UpdateContainerExpirationPolicyInput!) {
  updateContainerExpirationPolicy(input: $input) {
    containerTagsExpirationPolicy {
      ...ContainerTagsExpirationPolicyFields
    }
    errors
  }
}
