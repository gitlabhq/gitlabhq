type FlowMetricsCommits {
  unit: String!
  value: Int!
  identifier: String!
  links: [String]
  title: String!
}

extend type Query {
  flowMetricsCommits(
    fullPath: ID!
    from: Time!
    to: Time!
    labelNames: [String!]
  ): FlowMetricsCommits
}
