fragment DoraMetricItem on DoraMetric {
  date
  deployment_frequency: deploymentFrequency @skip(if: $skipDeploymentFrequency)
  change_failure_rate: changeFailureRate @skip(if: $skipChangeFailureRate)
  lead_time_for_changes: leadTimeForChanges @skip(if: $skipLeadTimeForChanges)
  time_to_restore_service: timeToRestoreService @skip(if: $skipTimeToRestoreService)
}
