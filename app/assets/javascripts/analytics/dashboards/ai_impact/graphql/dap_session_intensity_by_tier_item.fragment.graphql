fragment DapSessionIntensityByTierItem on Analytics {
  duoWorkflows(createdAtFrom: $startDate, createdAtTo: $endDate) {
    aggregated {
      nodes {
        dimensions {
          userTier(thresholds: $thresholds)
        }
        totalCount
        usersCount
      }
    }
  }
}
