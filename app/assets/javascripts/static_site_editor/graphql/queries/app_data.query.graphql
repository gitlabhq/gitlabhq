query appData {
  appData @client {
    isSupportedContent
    hasSubmittedChanges
    project
    sourcePath
    username
    returnUrl
    mounts {
      source
      target
    }
    imageUploadPath
  }
}
