mutation RestorePagesDeployment($deploymentId: PagesDeploymentID!) {
  restorePagesDeployment(input: { id: $deploymentId }) {
    errors
    pagesDeployment {
      id
      active
      deletedAt
      updatedAt
    }
  }
}
