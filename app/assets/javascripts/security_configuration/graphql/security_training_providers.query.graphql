query getSecurityTrainingProviders($fullPath: ID!) {
  project(fullPath: $fullPath) {
    id
    securityTrainingProviders {
      name
      id
      description
      isEnabled
      url
    }
  }
}
