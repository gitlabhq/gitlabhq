query abuseReportQuery($id: AbuseReportID!) {
  abuseReport(id: $id) {
    id
    labels {
      nodes {
        id
        title
        description
        color
        textColor
      }
    }
  }
}
