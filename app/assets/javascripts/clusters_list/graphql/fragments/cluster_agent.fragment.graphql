fragment ClusterAgentFragment on ClusterAgent {
  id
  name
  webPath
  createdAt
  userAccessAuthorizations {
    config
  }
  connections {
    nodes {
      metadata {
        version
      }
    }
  }
  tokens {
    nodes {
      id
      lastUsedAt
    }
  }
}
