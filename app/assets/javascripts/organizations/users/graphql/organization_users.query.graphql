query getOrganizationUsers($id: OrganizationsOrganizationID!) {
  organization(id: $id) {
    id
    organizationUsers {
      nodes {
        badges
        id
        user {
          id
        }
      }
    }
  }
}
