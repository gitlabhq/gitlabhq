mutation organizationUserUpdate($input: OrganizationUserUpdateInput!) {
  organizationUserUpdate(input: $input) {
    organizationUser {
      id
      accessLevel {
        stringValue
      }
    }
    errors
  }
}
