query getCurrentUserOrganizationsClient {
  currentUser {
    id
    organizations @client {
      nodes {
        id
        name
        descriptionHtml
        avatarUrl
        webUrl
      }
    }
  }
}
