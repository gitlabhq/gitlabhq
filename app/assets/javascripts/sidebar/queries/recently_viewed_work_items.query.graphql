# @feature_category: portfolio_management
query recentlyViewedWorkItems {
  currentUser {
    id
    recentlyViewedIssues {
      id
      title
      webUrl
      reference(full: true)
      workItemType {
        id
        name
        iconName
      }
    }
  }
}
