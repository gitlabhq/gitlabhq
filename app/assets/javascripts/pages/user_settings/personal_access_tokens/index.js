import {
  initAccessTokenTableApp,
  initExpiresAtField,
  initNewAccessTokenApp,
  initSharedAccessTokenApp,
  initTokensApp,
} from '~/access_tokens';

initAccessTokenTableApp();
initExpiresAtField();
initTokensApp();
initSharedAccessTokenApp();
initNewAccessTokenApp();
