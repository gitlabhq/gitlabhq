fragment CatalogResourceFields on CiCatalogResource {
  id
  webPath
  icon
  name
  description
  starCount
  latestVersion {
    id
    components {
      nodes {
        id
        name
      }
    }
    name
    path
    releasedAt
    author {
      id
      name
      webUrl
    }
  }
}
