query getAllPipelinesCount(
  $fullPath: ID!
  $source: String
  $ref: String
  $username: String
  $status: PipelineStatusEnum
) {
  project(fullPath: $fullPath) {
    id
    pipelines(source: $source, ref: $ref, username: $username, status: $status) {
      count
    }
  }
}
