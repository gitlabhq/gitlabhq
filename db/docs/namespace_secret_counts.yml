---
table_name: namespace_secret_counts
classes:
- SecretsManagement::NamespaceSecretCount
feature_categories:
- secrets_management
description: Tracks the count of secrets stored in OpenBao for each group or project namespace that has an active secrets manager. Refreshed asynchronously after secret create/delete and reconciled by a daily cron.
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/234604
milestone: '19.1'
gitlab_schema: gitlab_main_org
sharding_key:
  namespace_id: namespaces
table_size: small
