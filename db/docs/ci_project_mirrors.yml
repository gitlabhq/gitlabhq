---
table_name: ci_project_mirrors
classes:
- Ci::ProjectMirror
feature_categories:
- cell
description: Mirrors some data from the `main` database into the `ci` database so
  that we can join directly in a single query
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/75621
milestone: '14.6'
gitlab_schema: gitlab_ci
sharding_key:
  project_id: projects
table_size: small
