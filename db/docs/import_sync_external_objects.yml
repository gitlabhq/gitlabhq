---
table_name: import_sync_external_objects
classes:
- Import::Sync::ExternalObject
feature_categories:
- importers
description: Maps one continuously-synced GitHub object to the GitLab issue, merge
  request, or note it produced, so replayed provider updates converge on the same
  local record.
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/255079
milestone: '19.5'
gitlab_schema: gitlab_main_org
sharding_key:
  project_id: projects
table_size: small
