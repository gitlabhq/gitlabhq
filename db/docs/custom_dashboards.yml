---
table_name: custom_dashboards
classes:
- Analytics::CustomDashboards::Dashboard
feature_categories:
- custom_dashboards_foundation
description: |
  Stores dashboards used across multiple GitLab features.
  Each record represents a customizable dashboard configuration stored in JSONB
  format. Supports versioning, optimistic locking, auditing, and search
  integration via the `custom_dashboard_search_data` table.
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/211458
milestone: '18.6'
gitlab_schema: gitlab_main_org
sharding_key:
  organization_id: organizations
table_size: small
