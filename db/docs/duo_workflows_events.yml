---
table_name: duo_workflows_events
classes:
- Ai::DuoWorkflows::Event
feature_categories:
- duo_agent_platform
description: Stores control and message events (pause, resume, stop, message, response,
  require_input) exchanged with Duo Workflow runs, along with delivery status.
introduced_by_url:
milestone: '17.5'
gitlab_schema: gitlab_main_org
sharding_key:
  project_id: projects
  namespace_id: namespaces
table_size: small
