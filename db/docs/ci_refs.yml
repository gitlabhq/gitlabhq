---
table_name: ci_refs
classes:
- Ci::Ref
feature_categories:
- continuous_integration
description: Tracks the latest CI/CD health status (success, failed, fixed, broken,
  still_failing) per Git ref so pipeline regressions can be detected.
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/16951
milestone: '12.9'
gitlab_schema: gitlab_ci
sharding_key:
  project_id: projects
table_size: medium
