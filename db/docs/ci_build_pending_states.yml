---
table_name: ci_build_pending_states
classes:
- Ci::BuildPendingState
feature_categories:
- continuous_integration
description: Stores a snapshot of each CI build's final state (status, trace CRC32
  checksum, failure reason) reported by the runner, used to reconcile build completion.
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/41585
milestone: '13.4'
gitlab_schema: gitlab_ci
table_size: small
sharding_key:
  project_id: projects
