---
table: ci_runner_projects
database: ci
replication_targets:
  - name: clickhouse_main
    target: siphon_ci_runner_projects
    dedup_by:
      - id
    reconcile:
      column: traversal_path
      expression_key_columns:
        - project_id
