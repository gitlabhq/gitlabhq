---
table: ci_runner_namespaces
database: ci
replication_targets:
  - name: clickhouse_main
    target: siphon_ci_runner_namespaces
    dedup_by:
      - id
    reconcile:
      column: traversal_path
      expression_key_columns:
        - namespace_id
