---
table: events
database: main
replication_targets:
  - name: clickhouse_main
    target: siphon_events
    dedup_by:
      - id
    reconcile:
      column: path
      expression_key_columns:
        - group_id
        - personal_namespace_id
        - project_id
