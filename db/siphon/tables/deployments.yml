---
table: deployments
database: main
replication_targets:
  - name: clickhouse_main
    target: siphon_deployments
    dedup_by:
      - id
    reconcile:
      column: traversal_path
      expression_key_columns:
        - project_id
