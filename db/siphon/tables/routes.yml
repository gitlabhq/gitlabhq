---
table: routes
database: main
replication_targets:
  - name: clickhouse_main
    target: siphon_routes
    dedup_by:
      - id
    reconcile:
      column: traversal_path
      expression_key_columns:
        - namespace_id
