---
table: p_ci_builds
database: ci
ignored_columns:
  - lock_version
  - description
  - coverage_regex
  - token_encrypted
replication_targets:
  - name: clickhouse_main
    target: siphon_p_ci_builds
    dedup_by_columns_lookup_table: siphon_p_ci_builds_pg_pkey_ordered
    dedup_by:
      - id
      - partition_id
    reconcile:
      column: traversal_path
      expression_key_columns:
        - project_id
