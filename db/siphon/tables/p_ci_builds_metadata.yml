---
table: p_ci_builds_metadata
database: ci
ignored_columns:
- secrets
- id_tokens
replication_targets:
- name: clickhouse_main
  target: siphon_p_ci_builds_metadata
  dedup_by:
  - id
  - partition_id
  reconcile:
    column: traversal_path
    expression_key_columns:
    - project_id
