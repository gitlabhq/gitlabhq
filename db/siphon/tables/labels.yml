---
table: labels
database: main
ignored_columns:
  - description_html
replication_targets:
  - name: clickhouse_main
    target: siphon_labels
    dedup_by:
      - id
    reconcile:
      column: traversal_path
      expression_key_columns:
        - group_id
        - organization_id
        - project_id
