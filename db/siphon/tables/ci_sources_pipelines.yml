---
table: ci_sources_pipelines
database: ci
replication_targets:
- name: clickhouse_main
  target: siphon_ci_sources_pipelines
  dedup_by:
  - id
  reconcile:
    column: traversal_path
    expression_key_columns:
    - project_id
