name: gitlab_shared
description:
  (Deprecated - use gitlab_shared_cell_local or gitlab_shared_org instead) Schema for all tables implementing shared features,
  ex. loose foreign keys, re-indexing, etc.
allow_cross_joins:
  - gitlab_internal
allow_cross_transactions:
  - gitlab_internal
require_sharding_key: false
