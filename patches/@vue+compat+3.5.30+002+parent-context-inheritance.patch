diff --git a/node_modules/@vue/compat/dist/vue.cjs.js b/node_modules/@vue/compat/dist/vue.cjs.js
index 4099618..c9dc4d5 100644
--- a/node_modules/@vue/compat/dist/vue.cjs.js
+++ b/node_modules/@vue/compat/dist/vue.cjs.js
@@ -7554,6 +7554,23 @@ function createCompatVue$1(createApp, createSingletonApp) {
     if (Ctor !== Vue) {
       applySingletonPrototype(app, Ctor);
     }
+    if (options.parent && options.parent.$) {
+      // GitLab specific: inherit provide/inject context if `parent` was passed - used for overlays
+      if (options.parent.$.provides) {
+        app._context.provides = Object.create(options.parent.$.provides);
+      }
+      // GitLab specific: inherit globalProperties (for Vuex $store, etc.)
+      // Note: We must copy properties, not use prototype chain, because Vue uses hasOwn() checks
+      const parentAppContext = options.parent.$.appContext;
+      const parentGlobalProps = parentAppContext && parentAppContext.config && parentAppContext.config.globalProperties;
+      if (parentGlobalProps) {
+        for (const key of Object.keys(parentGlobalProps)) {
+          if (!(key in app.config.globalProperties)) {
+            app.config.globalProperties[key] = parentGlobalProps[key];
+          }
+        }
+      }
+    }
     const vm = app._createRoot(options);
     if (options.el) {
       return vm.$mount(options.el);
diff --git a/node_modules/@vue/compat/dist/vue.runtime.esm-bundler.js b/node_modules/@vue/compat/dist/vue.runtime.esm-bundler.js
index 8b0852f..17012e2 100644
--- a/node_modules/@vue/compat/dist/vue.runtime.esm-bundler.js
+++ b/node_modules/@vue/compat/dist/vue.runtime.esm-bundler.js
@@ -7463,6 +7463,23 @@ function createCompatVue$1(createApp, createSingletonApp) {
     if (Ctor !== Vue) {
       applySingletonPrototype(app, Ctor);
     }
+    if (options.parent && options.parent.$) {
+      // GitLab specific: inherit provide/inject context if `parent` was passed - used for overlays
+      if (options.parent.$.provides) {
+        app._context.provides = Object.create(options.parent.$.provides);
+      }
+      // GitLab specific: inherit globalProperties (for Vuex $store, etc.)
+      // Note: We must copy properties, not use prototype chain, because Vue uses hasOwn() checks
+      const parentAppContext = options.parent.$.appContext;
+      const parentGlobalProps = parentAppContext && parentAppContext.config && parentAppContext.config.globalProperties;
+      if (parentGlobalProps) {
+        for (const key of Object.keys(parentGlobalProps)) {
+          if (!(key in app.config.globalProperties)) {
+            app.config.globalProperties[key] = parentGlobalProps[key];
+          }
+        }
+      }
+    }
     const vm = app._createRoot(options);
     if (options.el) {
       return vm.$mount(options.el);
