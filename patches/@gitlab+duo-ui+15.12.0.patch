diff --git a/node_modules/@gitlab/duo-ui/dist/components/chat/markdown_renderer.js b/node_modules/@gitlab/duo-ui/dist/components/chat/markdown_renderer.js
index d9867dd..fb48648 100644
--- a/node_modules/@gitlab/duo-ui/dist/components/chat/markdown_renderer.js
+++ b/node_modules/@gitlab/duo-ui/dist/components/chat/markdown_renderer.js
@@ -13,22 +13,37 @@ const baseOptions = {
 const baseRenderer = new Renderer({
   ...baseOptions
 });
+const CODE_BLOCK_WRAPPER_OPEN = '<div class="gl-relative gl-flex js-duo-markdown-code duo-message-pre-block !focus:gl-focus" tabindex="0">\n<duo-copy-code></duo-copy-code>\n<duo-insert-code-snippet></duo-insert-code-snippet>\n<pre class="code code-syntax-highlight-theme duo-message-pre-block gl-grow gl-overflow-auto gl-grid">';
 const customCodeRenderer = {
   code() {
-    const baseCode = baseRenderer.code(...arguments);
-
-    // Insert copy-code elements inside the <pre> tag
-    return baseCode.replace('<pre>', '<div class="gl-relative gl-flex js-markdown-code duo-message-pre-block !focus:gl-focus" tabindex="0">\n<copy-code></copy-code>\n<insert-code-snippet></insert-code-snippet>\n<pre class="code code-syntax-highlight-theme duo-message-pre-block gl-grow gl-overflow-auto gl-grid">').replace('</pre>', '</pre></div>');
+    return baseRenderer.code(...arguments).replace('<pre>', CODE_BLOCK_WRAPPER_OPEN).replace('</pre>', '</pre></div>');
   }
 };
 const duoMarked = new Marked([{
   ...baseOptions
 }, markedBidi()]);
 const config = {
-  ADD_TAGS: ['insert-code-snippet', 'copy-code', 'gl-markdown', '#text', 'gl-compact-markdown'],
-  ADD_ATTR: ['data-canonical-lang', 'data-sourcepos', 'lang', 'data-src', 'img'],
+  ADD_TAGS: ['duo-insert-code-snippet', 'duo-copy-code', '#text'],
+  ADD_ATTR: ['data-href'],
   FORBID_TAGS: ['script', 'style', 'iframe', 'form', 'button', 'svg', 'video', 'audio', 'embed', 'object'],
-  FORBID_ATTR: ['onerror', 'onload', 'onclick']
+  FORBID_ATTR: ['onerror', 'onload', 'onclick', 'id', 'style'],
+  ALLOW_DATA_ATTR: false
+};
+
+// Allow known-safe class tokens (highlight.js + customCodeRenderer wrapper);
+// strip everything else so agent HTML can't inject GitLab JS gadget classes.
+const SAFE_CLASS_TOKEN_RE = /^(?:hljs(?:-[\w-]+)?|language-[\w-]+)$/;
+const ALLOWED_WRAPPER_CLASSES = new Set(Array.from(CODE_BLOCK_WRAPPER_OPEN.matchAll(/class="([^"]+)"/g)).flatMap(m => m[1].split(/\s+/).filter(Boolean)));
+const sanitizeClassAttributeHook = (node, data) => {
+  if (data.attrName !== 'class') return;
+  const safe = data.attrValue.split(/\s+/).filter(t => t && (SAFE_CLASS_TOKEN_RE.test(t) || ALLOWED_WRAPPER_CLASSES.has(t)));
+  if (safe.length === 0) {
+    // eslint-disable-next-line no-param-reassign
+    data.keepAttr = false;
+  } else {
+    // eslint-disable-next-line no-param-reassign
+    data.attrValue = safe.join(' ');
+  }
 };
 const handleImageElements = node => {
   var _node$nodeName;
@@ -113,32 +128,17 @@ const sanitizeLinksHook = function () {
     node.removeAttribute('href');
   };
 };
-function isHtml(markup) {
-  const src = markup.toString().trim();
-  if (src.length === 0 || !src.startsWith('<') || !src.includes('>')) {
-    return false; // fast-fail trivial cases
-  }
-  const doc = new DOMParser().parseFromString(src, 'text/html');
-
-  // DOMParser drops invalid tags but inserts a <parsererror> element on failure
-  // https://developer.mozilla.org/en-US/docs/Web/API/DOMParser/parseFromString#error_handling
-  if (doc.querySelector('parsererror') !== null) {
-    return false;
-  }
-
-  // Do we have at least one real element node inside <body>?
-  return Array.from(doc.body.childNodes).some(n => n.nodeType === Node.ELEMENT_NODE);
-}
 async function renderDuoChatMarkdownPreview(md) {
-  let {
-    trustedUrls = []
-  } = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
+  let _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
+    _ref$trustedUrls = _ref.trustedUrls,
+    trustedUrls = _ref$trustedUrls === void 0 ? [] : _ref$trustedUrls;
   if (!md) return '';
   DOMPurify.addHook('beforeSanitizeElements', handleImageElements);
+  DOMPurify.addHook('uponSanitizeAttribute', sanitizeClassAttributeHook);
   DOMPurify.addHook('afterSanitizeAttributes', sanitizeLinksHook(trustedUrls));
-  const parsedMarkdown = isHtml(md) ? md : await duoMarked.parse(md.toString());
-  const sanitized = DOMPurify.sanitize(parsedMarkdown, config);
+  const sanitized = DOMPurify.sanitize(await duoMarked.parse(md.toString()), config);
   DOMPurify.removeHook('beforeSanitizeElements');
+  DOMPurify.removeHook('uponSanitizeAttribute');
   DOMPurify.removeHook('afterSanitizeAttributes');
   return sanitized;
 }
