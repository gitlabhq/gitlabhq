inherit_from:
  - ../config/rubocop.yml

# The gem's specs run against a throwaway database they create themselves,
# so the suite has to establish its own connection.
Database/EstablishConnection:
  Exclude:
    - 'spec/support/database_helper.rb'
