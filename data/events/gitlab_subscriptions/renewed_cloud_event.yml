---
event: GitlabSubscriptions::RenewedCloudEvent
description: CloudEvent published when a GitLab subscription is renewed. Fires only
  on genuine renewals - both start_date and end_date must change in the same update,
  with the new start_date >= the previous end_date.
feature_category: subscription_management
