metadata:
  name:
  namespace:
  labels:
    environment:
spec:
  volumes:
  - name: repo
    emptyDir: {}
  containers:
  - name: fs
    image: micaelbergeron/gitlab-web-ide-fs
    ports:
    - containerPort: 9000
    volumeMounts:
      - name: repo
        mountPath: /export
    env:
      - name: REPO_URL
        value:
      - name: MINIO_ACCESS_KEY
        value: abcdefghijklm
      - name: MINIO_SECRET_KEY
        value: abcdefghijklm
  - name: app
    image:
    ports:
      - containerPort:
    volumeMounts:
      - name: repo
        mountPath: /app
