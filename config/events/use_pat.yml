---
description: API request authenticated using a Personal Access Token
internal_events: true
action: use_pat
identifiers:
  - user
additional_properties:
  label:
    description: API endpoint identifier
  pat_type:
    description: Type of Personal Access Token used ("legacy" or "granular")
  response_code:
    description: HTTP response status code after PAT usage
  denied_permissions:
    description: >-
      Comma-separated list of raw granular permission identifiers the token lacked when a granular
      authorization was denied during the request. Absent when no granular denial occurred.
product_group: authorization
product_categories:
  - permissions
milestone: '19.0'
introduced_by_url: 'https://gitlab.com/gitlab-org/gitlab/-/merge_requests/230954'
tiers:
  - free
  - premium
  - ultimate
