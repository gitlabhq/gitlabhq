---
description: Tracks when a package recipe_revision is deleted from the registry
internal_events: true
action: delete_recipe_revision_from_registry
identifiers:
  - project
  - namespace
  - user
additional_properties:
  label:
    description: The name of the package type
  property:
    description: The auth type. Either 'guest', 'user' or 'deploy_token'
  deploy_token_id:
    description: The hashed deploy token id
product_group: package_registry
milestone: '18.1'
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/186073
tiers:
  - free
  - premium
  - ultimate
