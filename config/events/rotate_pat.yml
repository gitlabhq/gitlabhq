---
description: Rotation of a personal access token
internal_events: true
action: rotate_pat
identifiers:
- project
- namespace
- user
product_group: authentication
product_categories:
- system_access
milestone: '17.10'
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/184008
tiers:
- free
- premium
- ultimate
additional_properties:
  type:
    description: Type of the rotated Personal Access Token ("legacy" or "granular")
  creation_source:
    description: >-
      Source that rotated the token ("ui" for tokens rotated via Rails controllers; "api" for REST or
      GraphQL requests, including UI actions that use those transports; "unknown" when the service
      caller did not identify the source)
