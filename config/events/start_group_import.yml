---
description: A group import or migration started
internal_events: true
action: start_group_import
identifiers:
- namespace
- user
additional_properties:
  label:
    description: 'importer used for the group import (e.g., gitlab_group_export, gitlab_migration, offline_transfer)'
  property:
    description: hashed identifier of the source host and path, used to detect repeated imports of the same source
  request_channel:
    description: "How the migration was started: api, ui, or congregate (Professional Services tool identified by its user-agent)"
product_group: import
product_categories:
- importers
milestone: '19.4'
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/253927
tiers:
- free
- premium
- ultimate
