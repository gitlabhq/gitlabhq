---
description: A Direct Transfer or Offline Transfer migration request finished
internal_events: true
action: finish_gitlab_migration
identifiers:
- user
additional_properties:
  label:
    description: 'import_type of the bulk import (gitlab_migration or offline_transfer)'
  total_failure_count:
    description: total number of entity failures across every entity in the migration request
  duration_seconds:
    description: how long the entire migration request took, including queued time and every entity in it
  imported_objects_count:
    description: total number of objects imported across every entity in the migration request
  migrated_entities_count:
    description: number of entities (top-level and nested) in the migration request that finished successfully
product_group: import
product_categories:
- importers
milestone: '19.5'
introduced_by_url: https://gitlab.com/gitlab-org/gitlab/-/merge_requests/256998
tiers:
- free
- premium
- ultimate
