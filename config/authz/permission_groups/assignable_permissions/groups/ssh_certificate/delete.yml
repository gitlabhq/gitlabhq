---
name: delete_ssh_certificate
description: Grants the ability to delete SSH certificates
boundaries:
  - group
  - instance
available_for:
  - granular_access_token
permissions:
  - delete_ssh_certificate
assignable_when:
  - condition: admin
    boundaries:
      - instance
