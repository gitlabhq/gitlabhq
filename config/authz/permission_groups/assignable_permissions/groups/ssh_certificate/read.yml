---
name: read_ssh_certificate
description: Grants the ability to read SSH certificates
boundaries:
  - group
  - instance
available_for:
  - granular_access_token
permissions:
  - read_ssh_certificate
assignable_when:
  - condition: admin
    boundaries:
      - instance
