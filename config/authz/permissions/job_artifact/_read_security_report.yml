---
name: _read_security_report_job_artifact
description: Private permission that gates read access to job artifacts whose
  file_type is a security report (SAST, DAST, dependency scanning, etc.). The
  job artifact policy combines this permission with the artifact's file_type
  to enable the public read_job_artifacts permission for security report
  downloads. Must not be checked directly at enforcement points.
