{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Unique name for the permission"
    },
    "description": {
      "type": "string",
      "description": "Description of the permission"
    },
    "feature_category": {
      "type": "string",
      "description": "Feature category of the permission",
      "format": "known_product_category"
    },
    "action": {
      "type": "string",
      "description": "Override for the generated action name"
    },
    "resource": {
      "type": "string",
      "description": "Override for the generated resource name"
    },
    "available_for_tokens": {
      "type": "boolean",
      "description": "If set to `true`, allows the permission to be added to granular scopes for tokens"
    }
  },
  "required": [
    "name",
    "description",
    "feature_category"
  ]
}
