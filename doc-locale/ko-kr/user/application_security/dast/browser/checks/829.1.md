---
stage: Application Security Testing
group: Dynamic Analysis
info: To determine the technical writer assigned to the Stage/Group associated with this page, see <https://handbook.gitlab.com/handbook/product/ux/technical-writing/#assignments>
title: 신뢰할 수 없는 제어 영역에서의 기능 포함
---

## 설명 {#description}

JavaScript 또는 CSS 소스 파일이 [SRI(Sub-Resource Integrity)](https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Subresource_Integrity) 없이 제3자 도메인에서 포함됩니다. 공격자가 이러한 제3자 리소스를 호스팅하는 사이트를 손상시킨 경우, 애플리케이션의 사용자를 손상시키려는 시도로 악성 스크립트 또는 CSS 데이터를 주입할 수 있습니다. 그러나 SRI가 적용되었고 공격자가 스크립트의 내용을 수정하려고 시도한 경우, 브라우저는 스크립트를 로드하지 않으며 애플리케이션 사용자는 악성 변경으로부터 보호됩니다.

## 수정 {#remediation}

식별된 모든 리소스는 대상 애플리케이션과 동일한 도메인에서 제공되어야 합니다. 이것이 불가능한 경우 `script` 값을 구현하는 모든 `src` 태그 또는 `href` 값을 구현하는 `link` 태그에 Sub-Resource Integrity를 포함할 것을 강력히 권장합니다. SRI 무결성 값을 생성하려면 [SRI hash](https://www.srihash.org/) 도구를 사용하거나 다음 명령 중 하나를 실행합니다:

- `cat FILENAME.js | openssl dgst -sha384 -binary | openssl base64 -A`
- `shasum -b -a 384 FILENAME.js | awk '{ print $1 }' | xxd -r -p | base64`

이 도구의 출력은 추가 속성으로 추가되어야 하며, 특히 `integrity` 및 `crossorigin=anonymous` 또는 `crossorigin=use-credentials` 중 하나입니다. 유효한 SRI로 보호된 스크립트 태그의 예를 아래에서 확인할 수 있습니다:

```html
<script src="https://example.com/example-framework.js"
    integrity="sha384-oqVuAfXRKap7fdgcCY5uykM6+R9GqQ8K/uxy9rx7HNQlGYl1kPzQho1wx4JwY8wC"
    crossorigin="anonymous"></script>
```

## 세부 정보 {#details}

| ID | 집계됨 | CWE | 형식 | 위험 |
|:---|:-----------|:----|:-----|:-----|
| 829.1 | true | 829 | 수동 | 낮음 |

## 링크 {#links}

- [OWASP](https://cheatsheetseries.owasp.org/cheatsheets/Third_Party_Javascript_Management_Cheat_Sheet.html#subresource-integrity)
- [CWE](https://cwe.mitre.org/data/definitions/829.html)
- [MDN](https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Subresource_Integrity)
