#!/usr/bin/env ruby

# frozen_string_literal: true

require "bundler/setup"
require "gitlab/orchestrator/cli"

require "pry"

# This module just allows to not disable rubocop rule
module Console
  Pry.start
end
